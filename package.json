{
  "name": "ct_tool",
  "version": "1.2.2",
  "scripts": {
    "build": "npm run build:web && npm run build:local-server",
    "build:web": "npm --prefix frontend run build",
    "build:local-server": "node local-server/scripts/build.cjs",
    "local-server": "node local-server/server.cjs"
  }
}
